name: CI
on:
  pull_request:
    branches: [main]

jobs:
  validate-cheeses:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node tools/validate-cheeses.js
